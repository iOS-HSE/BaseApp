# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'TestApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestApp

  pod 'Firebase'
  pod 'Swinject'
  pod 'DependencyInjection', :path => "Modules/DependencyInjection/module.podspec"
  pod 'SDK', :path => "Modules/SDK/module.podspec"
  pod 'Persistency', :path => "Modules/Persistency/module.podspec"
  pod 'Core', :path => "Modules/Core/module.podspec"
  pod 'Logging', :path => "Modules/Logging/module.podspec"

end
